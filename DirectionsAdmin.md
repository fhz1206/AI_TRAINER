# 统一训练平台 — 管理员使用说明

> 管理员面板使用指南，涵盖用户管理、权限控制、日志审计、设备监控等功能。

---

## 目录

- [管理员账户](#管理员账户)
- [访问管理员面板](#访问管理员面板)
- [用户管理](#用户管理)
  - [查看用户列表](#查看用户列表)
  - [修改用户角色](#修改用户角色)
  - [管理用户分组](#管理用户分组)
  - [重置用户密码](#重置用户密码)
  - [删除用户](#删除用户)
  - [搜索用户](#搜索用户)
- [日志查看](#日志查看)
  - [查看行为日志](#查看行为日志)
  - [搜索日志](#搜索日志)
- [设备状态监控](#设备状态监控)
  - [查看设备信息](#查看设备信息)
  - [实时 CPU/RAM 监控](#实时-cpuram-监控)
- [安全机制](#安全机制)
  - [管理员权限保护](#管理员权限保护)
  - [操作审计](#操作审计)
- [常见问题](#常见问题)

---

## 管理员账户

### 默认管理员

平台初始化时自动创建管理员账户：

| 项目 | 内容 |
|------|------|
| 用户名 | `admin` |
| 初始密码 | `123456` |

> ⚠️ 首次登录后请立即修改密码，确保账户安全。

### 管理员权限说明

管理员账户拥有以下特殊权限：

- 访问 `/admin` 管理员面板
- 查看所有用户信息
- 修改任意用户的角色（管理员/普通用户）
- 管理用户分组
- 重置非管理员用户的密码
- 删除非管理员用户
- 查看所有用户的行为日志
- 查看服务器设备状态和实时监控

### 管理员身份保护机制

管理员身份的判定基于 **用户名** 而非用户 ID，防止因修改用户名导致权限丢失：

- 只有 `username = 'admin'` 且 `role = 'admin'` 的用户才被视为管理员
- 即使修改了 admin 账户的用户名，系统仍会通过 `username` 字段判断
- 管理员账户无法被删除
- 管理员账户的密码无法被其他管理员重置（需原密码自行修改）

---

## 访问管理员面板

### 入口

1. 使用 `admin` 账户登录平台
2. 在导航栏中点击 **🛡️ 管理员** 按钮
3. 或直接在浏览器地址栏输入 `http://127.0.0.1:5000/admin`

### 权限验证

访问管理员面板需要满足两个条件：

1. 已登录（`@login_required`）
2. 当前用户是管理员（`@admin_required`，即 `username='admin'`）

如果普通用户尝试访问管理员面板，会返回 403 权限错误。

---

## 用户管理

### 查看用户列表

管理员面板默认显示 **👥 用户管理** 标签页，以表格形式展示所有用户信息：

| 列 | 说明 |
|------|------|
| ID | 用户编号 |
| 用户名 | 用户登录名 |
| 角色 | 管理员/普通用户（可下拉切换） |
| 分组 | 用户所属分组（可编辑） |
| 注册时间 | 账户创建时间 |
| 操作 | 重置密码、删除用户按钮 |

**顶部统计卡片：**
- 总用户数
- 管理员数量
- 普通用户数量

### 修改用户角色

1. 在用户列表中找到目标用户
2. 点击该用户所在行的"角色"下拉框
3. 选择 **管理员** 或 **用户**
4. 系统自动保存并弹出 ✅ 角色已更新 提示

> 注意：管理员账户（admin）的角色下拉框为禁用状态，不可修改。

### 管理用户分组

1. 在用户列表中找到目标用户
2. 在"分组"列的输入框中输入或修改分组名称
3. 点击旁边的 **💾 保存** 按钮
4. 系统自动保存并弹出 ✅ 分组已保存 提示

分组功能可用于：
- 按部门或团队对用户进行分类
- 按权限级别对用户进行分组管理
- 通过搜索功能快速筛选特定分组的用户

### 重置用户密码

1. 在用户列表中找到目标用户
2. 点击该用户所在行的 **🔑 改密码** 按钮
3. 在弹出的对话框中输入新密码（至少4个字符）
4. 点击 **确认重置** 按钮
5. 系统自动保存并弹出 🔑 密码已重置 提示

> 注意：管理员账户的改密码按钮为禁用状态，不可被其他管理员重置。

### 删除用户

1. 在用户列表中找到目标用户
2. 点击该用户所在行的 **🗑️ 删除** 按钮
3. 系统弹出确认对话框，提示"确定要删除用户「xxx」吗？此操作不可撤销！"
4. 点击确定后，用户及其关联数据（文件记录、模型记录、行为日志）将被级联删除
5. 系统自动弹出 🗑️ 用户已删除 提示

**删除限制：**
- 管理员账户（admin）不可删除
- 当前登录的管理员自身不可删除
- 删除操作不可撤销，请谨慎操作

### 搜索用户

在搜索框中输入关键词，系统会实时过滤用户列表：

- 支持按 **用户名** 搜索（模糊匹配）
- 支持按 **分组名** 搜索（模糊匹配）
- 搜索结果实时更新，无需点击搜索按钮

---

## 日志查看

### 查看行为日志

管理员面板的 **📋 日志查看** 标签页展示所有用户的行为日志，按时间倒序排列：

**每条日志包含：**
- 时间戳 — 操作发生的具体时间
- 用户名 — 执行操作的用户
- 操作类型 — 归类标签（上传/训练/测试/管理/账号）
- 操作描述 — 详细的操作内容

**操作类型图标：**

| 图标 | 类型 | 说明 |
|------|------|------|
| 🟢 | upload | 上传数据集 |
| 🔵 | train | 训练模型 |
| 🟡 | test | 运行测试 |
| 🔴 | admin | 管理操作 |
| ⚪ | profile | 账号修改 |

### 搜索日志

在搜索框中输入关键词，系统会实时过滤日志列表：

- 支持按 **操作描述** 搜索
- 支持按 **用户名** 搜索
- 支持按 **操作类型** 搜索
- 搜索结果实时更新

---

## 设备状态监控

### 查看设备信息

管理员面板的 **🖥️ 设备状态** 标签页展示服务器的实时硬件信息：

**系统信息卡片：**
- 操作系统名称和版本
- Python 版本

**硬件信息卡片：**

| 卡片 | 显示内容 |
|------|----------|
| 🔲 CPU | 型号（核心数/线程数）、使用率、频率、支持状态 |
| 🧠 RAM | 总容量、已用、可用、使用率 |
| 💾 硬盘（启动目录） | 总容量、已用、剩余、使用率 |
| 🎮 GPU | 型号、显存占用、支持状态（标注不支持） |
| ⚡ XPU | 支持状态（标注不支持） |
| 🔬 NPU | 支持状态（标注不支持） |

### 实时 CPU/RAM 监控

1. 在设备状态页面的 CPU 或 RAM 卡片上点击 **📈 查看详情** 按钮
2. 弹出实时监控弹窗，显示：
   - **CPU 使用率** 和 **RAM 使用率** 的实时数值
   - **CPU 使用率趋势图** — 红色折线图，实时更新
   - **RAM 使用率趋势图** — 紫色折线图，实时更新
3. 图表每 1 秒自动刷新一次，最多显示 60 个数据点（1 分钟趋势）
4. 点击 **关闭 ✕** 按钮或点击弹窗外部区域关闭监控

---

## 安全机制

### 管理员权限保护

- 管理员身份基于 `username='admin'` 判断，而非用户 ID
- 即使修改了 admin 的 username，系统仍能正确识别管理员身份
- 管理员账户不可被删除
- 管理员密码不可被其他管理员重置
- 所有管理员操作都会记录到行为日志

### 操作审计

所有管理员操作都会被记录到 `activity_logs` 表中，包括：

| 操作 | 日志内容示例 |
|------|-------------|
| 修改角色 | "修改用户 testuser 角色为 admin" |
| 修改分组 | "将用户 testuser 分到组 研发部" |
| 删除用户 | "删除用户 testuser" |
| 重置密码 | "重置用户 testuser 的密码" |
| 修改密码 | "修改了登录密码" |
| 修改用户名 | "修改用户名为 newname" |

这些日志在管理员面板的日志查看页面中可随时查阅。

---

## 常见问题

### Q: 忘记了 admin 密码怎么办？

A: 管理员密码无法通过面板重置。需要联系服务器管理员直接修改数据库：

```sql
-- 在 SQLite 数据库中执行（需要服务器文件系统权限）
UPDATE users SET password_hash = '新密码的SHA256哈希' WHERE username = 'admin';
```

或删除 `training_platform.db` 文件重启服务，系统会自动重新创建 admin 账户（密码 `123456`）。

### Q: 普通用户能否看到管理员面板？

A: 不能。访问 `/admin` 需要满足 `@admin_required` 装饰器条件，普通用户访问会返回 403 错误。

### Q: 如何查看某个特定用户的所有操作？

A: 在管理员面板的日志查看页面，搜索框中输入该用户的用户名，即可过滤出该用户的所有操作记录。

### Q: 设备监控数据多久更新一次？

A: 设备状态页面的基本数据在页面加载时获取一次。实时监控弹窗中的图表每 1 秒刷新一次。

### Q: 为什么 GPU/XPU/NPU 显示为不支持？

A: 当前环境未检测到对应的硬件或驱动。如果安装了 CUDA 且 PyTorch 能检测到 GPU，GPU 状态会自动变为"支持"并显示 GPU 型号和显存信息。

---

> 本文档最后更新于 2026年7月